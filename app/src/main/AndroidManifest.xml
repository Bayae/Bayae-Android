<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dudes.dexin.bayae">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/myThemeStyle">
        <uses-library android:name="org.apache.http.legacy" android:required="false" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.Register"></activity>
        <activity android:name=".user.Login" />
        <activity android:name=".user.User_Info" />
        <activity android:name=".study.ModelSelect"></activity>
        <activity android:name=".game.game_select">
            <intent-filter>
                <action android:name="android.intent.action.GAME_SELET" />
                <category android:name="android.intent.category.GAME_SELET" />
            </intent-filter>
        </activity>
        <activity android:name=".game.game_main"></activity>
        <activity android:name=".game.game"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />



</manifest>